[build]
builder = "NIXPACKS"

[deploy]
startCommand = "HEALTH_PORT=$PORT python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 30